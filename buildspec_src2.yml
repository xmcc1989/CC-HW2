version: 0.2
phases:
 install:
  runtime-versions:
   python: 3.7
 build: 
  commands:
   - ls
   - cd $CODEBUILD_SRC_DIR/src2
   - sam build
   - export BUCKET=b0deploy
   - sam package --s3-bucket $BUCKET --output-template-file outputtemplate_src2.yml
artifacts:
 type: zip
 files:
  - template_src2.yml
  - outputtemplate_src2.yml